import { Inter, Roboto_Mono, Montserrat, Raleway } from '@next/font/google'

export const inter = Inter({
  subsets: ['latin'],
  display: 'optional',
})

export const raleway = Raleway({
  subsets: ['latin'],
  display: 'optional',
})
